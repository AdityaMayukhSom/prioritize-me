import { LoginForm } from "./LoginForm"

import { } from "react"
export const LoginPage = () => {
    return (
        // for background image related query, check App.css with its ID
        <div id="login-page" className="flex flex-col items-center justify-center w-screen h-screen">
            <LoginForm />
        </div>
    )
}